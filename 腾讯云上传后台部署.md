# 腾讯云 COS 作品直传设置

## 当前结构

- 公开网站：GitHub Pages。
- 私人管理页：`/admin.html`，不出现在公开导航，并设置 `noindex`。
- 上传方式：管理页在当前浏览器会话内连接腾讯云 COS。
- 作品目录：`portfolio/catalog/works.json`。
- 密钥处理：只保留在当前页面内存中，关闭或刷新页面即清除，不写入 GitHub、配置文件、LocalStorage 或 SessionStorage。
- 不使用腾讯云函数。

## 一次性 COS 配置

1. 存储桶：`maoyueyuan-1474173929`。
2. 地域：`ap-guangzhou`。
3. 访问权限保持“公有读、私有写”，禁止公有写。
4. 进入“安全管理 → 跨域访问 CORS 设置”，添加规则：
   - 来源 Origin：`https://maoyueyuan17337047035-spec.github.io`
   - 允许的方法：`GET`、`HEAD`、`PUT`、`POST`
   - Allow-Headers：`*`
   - Expose-Headers：`ETag`、`Content-Length`、`x-cos-request-id`
   - Max-Age：`600`
   - 开启 `Vary: Origin`

## 日常上传

1. 打开 `https://maoyueyuan17337047035-spec.github.io/maoyueyuan/admin.html`。
2. 输入腾讯云 `SecretId` 和 `SecretKey`，连接工作台。
3. 填写作品信息，选择成片与封面。
4. 点击“上传并发布”。
5. 页面会依次上传视频、封面，并更新 `portfolio/catalog/works.json`；公开作品页会自动读取新作品。

## 密钥要求

- 已经公开、发送到聊天或写入代码的密钥必须立即禁用并删除。
- 建议使用独立子用户密钥，并遵循最小权限，只允许访问当前存储桶的 `portfolio/*` 路径。
- 大文件分块上传需要简单上传和分块上传相关权限；作品目录读取需要 `GetObject`，写入需要 `PutObject`。
- 管理页不会保存密钥，但永久密钥在浏览器前端使用仍存在风险。不要在公共或不受信任的设备上操作。
