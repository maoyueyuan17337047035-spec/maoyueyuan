# 腾讯云 COS 作品上传后台

## 最终结构

- 公开网站：GitHub Pages。
- 私人管理页：`/admin.html`，不出现在公开导航，并设置 `noindex`。
- 大文件：浏览器通过 15 分钟有效的预签名 URL 直接上传腾讯云 COS。
- 作品目录：`portfolio/catalog/works.json`。
- 云端权限：由云函数运行角色提供自动轮换的临时凭证，不创建永久 API 密钥。

## 一次性腾讯云配置

1. 确认存储桶完整名称和地域，默认配置为：
   - Bucket：`maoyueyuan-1474173929`
   - Region：`ap-guangzhou`
2. 将存储桶保持“公有读私有写”，不要开启公有写。
3. 在“安全管理 → 跨域访问 CORS”添加规则：
   - 来源：`https://maoyueyuan17337047035-spec.github.io`
   - 方法：`GET`、`HEAD`、`PUT`
   - Allow-Headers：`*`
   - Expose-Headers：`ETag`
   - Max-Age：`600`
   - 开启 `Vary: Origin`
4. 在 CAM 创建云函数运行角色，把 `cloud-functions/portfolio-admin/scf-cos-policy.json` 作为自定义策略关联到该角色。策略仅允许函数读写 `portfolio/*`。
5. 在广州地域创建 Node.js 18 或更高版本的事件函数，入口为 `index.main_handler`，上传 `cloud-functions/portfolio-admin` 打包后的代码，并选择上一步的运行角色。SCF 会自动注入临时凭证。
6. 为函数创建公网“函数 URL”，允许 `POST`、`GET`、`OPTIONS`，来源限定为上述 GitHub Pages 域名。
7. 配置函数环境变量：
   - `COS_BUCKET`
   - `COS_REGION`
   - `COS_PUBLIC_BASE_URL`
   - `ADMIN_PASSWORD_SALT`
   - `ADMIN_PASSWORD_HASH`
   - `SESSION_SECRET`
   - `ALLOWED_ORIGINS`
8. 将函数 URL 写入根目录 `portfolio-config.js` 的 `apiBaseUrl`。

## 管理密码与会话密钥

在本机终端进入函数目录后运行：

```sh
read -s "ADMIN_PASSWORD?管理密码（至少14位）: "
export ADMIN_PASSWORD
npm run secrets
unset ADMIN_PASSWORD
```

把输出的三个值填写到腾讯云函数环境变量。管理密码本身不需要保存到云函数。

## 最小权限范围

云函数运行角色只对以下路径授权读取、写入和查询对象：

`qcs::cos:ap-guangzhou:uid/1474173929:maoyueyuan-1474173929/portfolio/*`

需要的对象操作包括 `PutObject`、`GetObject`、`HeadObject`。不要授予删除整个存储桶、修改桶策略或访问其他桶的权限，也不要创建主账号永久密钥。
